{
  "$schema": "jarvos.project-context/v1",
  "type": "jarvos.project-context/v1",
  "purpose": "managed-software-stewardship",
  "audience": "jarvos-projects",
  "inputFields": ["findingId", "executionReference", "releaseReference", "visibility"],
  "receiptFields": ["type", "purpose", "audience", "destinationSelectors", "allowedVisibilities", "capabilityRevision", "activationEnvelopeDigest", "issuedAt", "expiresAt", "signature"],
  "projectionOutcomes": ["projected", "deduped", "denied", "unavailable"],
  "boundary": "The issuer and signer are external. A capability authorizes only its signed destination and visibility policy; each activity receives a derived opaque context key at projection time. This portable package exposes no signing material, private paths, source content, task state, or activation authorization."
}
